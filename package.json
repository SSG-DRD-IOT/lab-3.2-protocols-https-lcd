{
  "name": "rest_API_with_actuator",
  "description": "Restful HTTP API for controlling an LCDs backlight and text",
  "version": "0.0.0",
  "main": "main.js",
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node main.js"
  },
  "dependencies": {
    "express": "latest",
    "body-parser": "latest"
  }
}
